<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.king.mapper.RoleMapper">
  <resultMap id="BaseResultMap" type="top.king.entity.Role">
      <id column="role_id" javaType="java.lang.Integer" jdbcType="INTEGER" />
      <result column="rolename" javaType="java.lang.String" jdbcType="VARCHAR" />
      <result column="statement" javaType="java.lang.String" jdbcType="VARCHAR" />
      <result column="status" javaType="java.lang.String" jdbcType="CHAR" />
      <result column="role_code" javaType="java.lang.String" jdbcType="VARCHAR" />
  </resultMap>
  <insert id="insert" parameterType="top.king.entity.Role">
    insert into t_role (role_id, rolename, statement, 
      status, role_code)
    values (#{roleId,jdbcType=INTEGER}, #{rolename,jdbcType=VARCHAR}, #{statement,jdbcType=VARCHAR}, 
      #{status,jdbcType=CHAR}, #{roleCode,jdbcType=VARCHAR})
  </insert>
  <insert id="insertSelective" parameterType="top.king.entity.Role">
    insert into t_role
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="roleId != null">
        role_id,
      </if>
      <if test="rolename != null">
        rolename,
      </if>
      <if test="statement != null">
        statement,
      </if>
      <if test="status != null">
        status,
      </if>
      <if test="roleCode != null">
        role_code,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="roleId != null">
        #{roleId,jdbcType=INTEGER},
      </if>
      <if test="rolename != null">
        #{rolename,jdbcType=VARCHAR},
      </if>
      <if test="statement != null">
        #{statement,jdbcType=VARCHAR},
      </if>
      <if test="status != null">
        #{status,jdbcType=CHAR},
      </if>
      <if test="roleCode != null">
        #{roleCode,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>

  <!--从用户权限表中查询出用户拥有的权限列表-->
  <select id="selectRoles" parameterType="java.lang.String" resultType="java.lang.String">
    select role_code
    from t_role t
    where t.role_id in(
        select role_id from t_user_role where user_id=#{id}
    )
  </select>
</mapper>